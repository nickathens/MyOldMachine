{
  "weight": "heavy",
  "min_ram_gb": 4,
  "min_disk_gb": 3,
  "install_note": "Clones the upstream mempalace repo (~100 MB) and installs it editable into an isolated venv with ChromaDB + embedding model (~690 MB). Run setup script to install."
}
