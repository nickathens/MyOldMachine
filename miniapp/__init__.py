"""MyOldMachine Telegram Mini App server.

Optional FastAPI server exposing the bot's settings, schedule, projects, and
launch panels through Telegram's Mini App API. Bind 127.0.0.1; external
exposure is the tunnel's job (Tailscale Funnel, Cloudflare Tunnel, or LAN-only).
"""
